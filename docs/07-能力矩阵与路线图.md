# 活动实证功能目录与路线图

> 状态日期：2026-08-24。本文是当前用户可达、可由活动代码执行的统一实证功能目录。活动代码、测试、capability registry 与 `specs/capability-evidence.json` 优先于本文；`archive/` 中的资产和历史记录不能作为功能已上线的证据。

## 如何阅读本目录

- `active`：用户可从当前前端入口进入，真实数据可走到活动 R/Python 链路，结果以持久化报告或任务结果呈现。
- `experimental`：当前可执行，但只覆盖表中写明的规格切片；必须保留警告、诊断、实际执行方法和导出证据。
- `validated`：`validationEvidence` 已通过 contract、适用性和失败边界 gate；仍不等于整篇论文的主分析资格。
- `reviewer_ready`：额外通过独立 external oracle 与 numeric golden；仍需论文级 evidence manifest/publication gate。
- `publication_ready`：能力层只有在外部验证齐备且后续结果级发表门禁通过时才可能派生。当前没有活动切片处于此状态。
- `识别可用 / 估计未开放`：可识别、保存或导出设计，不能提交运行。
- `deferred` / `dormant`：不属于当前产品能力，不应在界面、报告或销售表述中写成可用。

所有分析都绑定数据版本、测量版本（如适用）、规格、随机种子和引擎输出。前端只格式化和展示结果，不重新计算统计量。

## 能力成熟度与论文资格

每个 capability slice 返回三个互不替代的字段：

| 字段 | 回答的问题 | 当前规则 |
| --- | --- | --- |
| `executionAvailable` | 当前 R/Python runner 能否执行？ | 是全局执行门槛，仍需当前上下文适用。 |
| `validationLevel` | validation evidence 完成到哪一层？ | `unvalidated`、`internally_validated`、`externally_validated`。 |
| `validationEvidence` | gate 使用哪些可复现证据？ | contract tests、applicability tests、failure fixtures、external oracle、numeric golden。 |
| `maturityLevel` | 当前切片完成到哪一层验证？ | `experimental`、`validated`、`reviewer_ready`、`publication_ready`。 |
| `publicationEligibility` | 能力或统计状态层是否具备继续审查的条件？ | `ineligible`、`conditional`、`eligible`；它不是最终发表批准。 |
| `publicationGate` | 这一次结果能否进入人工发表确认？ | 分别检查能力验证、单次运行证据、稿件披露；最后仍要求具名人工确认。 |

`executionAvailable=true` 绝不自动推出论文级资格；成熟度由 validation evidence gate 派生，不能仅把 slice ID 加入 allowlist。结果级 `publicationGate` 已实现三层 fail-closed 判定：能力切片必须全部 external、该次运行必须可靠且完成具体回放包的 clean-room verification、所有适用披露子集必须完整；三层全过后仍需具名带时间人工确认。当前运行时默认没有 clean-room 证明和人工确认，因此不会自动产生 `finalEligible=true`，能力目录也仍无 `publication_ready/eligible` 切片。

能力证据的机器可读单一来源为 `specs/capability-evidence.json`，并由 `specs/capability-evidence.schema.json` 约束。39 个活动切片逐项登记 estimand 状态、支持的数据结构、假设、诊断、oracle 独立性、测试证据、限制与版本；registry 的 validation evidence 直接从该 manifest 派生。schema、切片全集、结构边界或成熟度证据发生漂移时，API 门禁必须失败。`not_formally_frozen` 明确表示可执行切片尚无完整可认证估计目标，不能由通用描述补写为论文主分析资格。

## 入口总览

| 工作流 | 前端入口 | 是否需要活动数据集 | 当前状态 | 用途 |
| --- | --- | ---: | --- | --- |
| 数据与测量 | `数据与测量` | 是 | active | 导入、质检、变量确认、构念计分、测量版本 |
| 横截面问卷实证 | `横截面问卷 → 问卷与实证分析` | 是 | active | 描述、相关、测量、组间、回归和稳健性 |
| 条件过程 / SEM | `横截面问卷 → 条件过程模型` | 是 | active / 部分受限 | PROCESS 等价拓扑、OLS/logistic/SEM |
| 纵向面板 | `纵向面板 → 纵向面板分析` | 是 | active | CLPM、RI-CLPM、LCM-SR 与纵向测量 |
| 日记 / ESM | `日记 / ESM → 日记 / ESM 分析` | 是 | active | 二层模型、GLMM、多层中介与 DSEM |
| 随机实验设计与功效 | `随机实验设计与功效` | 实验设计需要；功效不需要 | experimental | 仅随机分配的组间/组内设计、ANCOVA、CR0 与声明 family 内的计划对比校正；不提供准实验因果识别 |
| 通用功效与精度 | `方法与功效工作台 → 通用功效与精度` | 否 | experimental | 样本量、效应量、精度、MDES、Monte Carlo |
| 通用多重插补 | `方法与功效工作台 → 通用多重插补` | 是 | experimental | MICE 数据集、线性回归 Rubin 合并 |
| 高级问卷测量 | `方法与功效工作台 → 高级问卷测量` | 是 | experimental | 声明 Target 的连续 ESEM、连续/有序 Bifactor、二元 2PL 与多分类 GRM IRT/DIF、CMB 诊断 |

顶层研究范式切换器只改变推荐路径和步骤顺序，不会把不适用的方法自动跑在当前数据上。高级工作台仅显示后端 `executionAvailable=true` 且至少含一个可执行切片的方法族，并同时展示成熟度与论文资格；“非随机比较”不会命中随机实验切片。

## 可执行证据索引

下表把目录条目与活动入口、registry/slice、跨语言规格、runner、结果字段和专项测试绑定。全部 `ACTIVE_CAPABILITIES` slice ID 由自动测试要求出现在本文；`project.manifest.json` 中登记的活动路径由架构门禁校验存在性。

| 目录条目 | 前端入口 | registry / 可执行 slice | 规格与 runner | 核心结果字段 | 专项验证 |
| --- | --- | --- | --- | --- | --- |
| 横截面问卷实证 | `EmpiricalAnalysis` → `EmpiricalAnalysisConfig` / 8 个结果分区 | `empirical.cross_sectional.overview`<br>`empirical.cross_sectional.measurement`<br>`empirical.cross_sectional.group_comparison`<br>`empirical.cross_sectional.hierarchical_regression`<br>`empirical.cross_sectional.response_surface` | HTTP request → `run_empirical_analysis.R` | `descriptives`, `correlations`, `efa`, `cfa`, `validity`, `aggregationDiagnostics`, `hierarchicalRegression` | `test_empirical_center.py`, R `test-statistical-conformance.R` |
| PROCESS / SEM | 模型画布 → `ModelEstimationEditor` / `SemStudio` | `model.process_catalog`<br>`model.sem` | `analysis-spec.schema.json` → `run_analysis.R` | 路径、间接效应、拟合、收敛/异常解 | `test_process_catalog.py`, R `test-process-goldens.R` |
| 纵向面板 | `EmpiricalAnalysis` → `EmpiricalAnalysisConfig` / `LongitudinalPanelConfig` → `LongitudinalMethodsSection` | `empirical.panel.clpm`<br>`empirical.panel.ri_clpm`<br>`empirical.panel.lcm_sr`<br>`empirical.panel.invariance`<br>`empirical.panel.ulmc_sensitivity` | 实证纵向规格 → `longitudinal_panel.R` / `longitudinal_latent.R` | `longitudinalPanel`, 等值性、缺失执行记录、功效 | `test_longitudinal_*.py`, R 纵向专项 |
| 日记 / ESM | `EmpiricalAnalysis` → `EmpiricalAnalysisConfig` / `DiaryMultilevelConfig` → `LongitudinalMethodsSection` | `empirical.diary.quality`<br>`empirical.diary.lmm`<br>`empirical.diary.glmm`<br>`empirical.diary.cross_classified_gaussian`<br>`empirical.diary.multilevel_mediation`<br>`empirical.diary.dsem`<br>`empirical.diary.mi`<br>`empirical.diary.power` | 实证日记规格 → `diary_*.R` | `diaryMultilevel`, GLMM/DSEM 诊断、缺失、功效 | `test_diary_*.py`, R 日记专项 |
| 随机实验设计 | `ExperimentWizard` | `experimental_design.glm_cluster.long.single_outcome`<br>`experimental_design.factorial_anova.long.single_outcome`<br>`experimental_design.ancova.long.single_outcome`<br>`experimental_design.repeated_measures.single_within`<br>`experimental_design.mixed_design.single_within` | `advanced-analysis-spec.schema.json` → `run_advanced_analysis.R` | `omnibusTests`, `estimatedMarginalMeans`, `contrasts`, `plannedContrastId`, `multiplicityFamilyId`, `pValueRaw`, `pValueAdjusted` | `test_experiment_formalization.py`, R `test-statistical-conformance.R`；仅随机分配，CR0 不冒充 CR2，未接入 participant-flow / CONSORT |
| 两层与聚合 | `MultilevelWizard` | `multilevel_model.aggregation.icc_rwg`<br>`multilevel_model.gaussian.two_level` | 高级规格 → `multilevel_aggregation.R` / `multilevel_runner.R` | ICC(1), ICC(2), design effect, rwg 零分布方差与 cluster 级证据 | `test_aggregation_diagnostics.py`, `test_multilevel_*.py` |
| 纵向高级模型 | 高级 API（`advanced-analyses`） | `longitudinal_model.observed_growth`<br>`longitudinal_model.traditional_clpm`<br>`longitudinal_model.ri_clpm`<br>`longitudinal_model.latent_growth`<br>`longitudinal_model.longitudinal_invariance` | `advanced-analysis-spec.schema.json` → `run_advanced_analysis.R` / `longitudinal_advanced.R` | 波次样本流、CLPM/RI-CLPM/LCM-SR/增长曲线/等值性、缺失执行记录 | `test_longitudinal_advanced.py`, R 纵向专项；`available_rows_ml` 未实现时显式拒绝 |
| 通用功效与精度 | `PowerWizard` | `power_analysis.analytic.regression`<br>`power_analysis.analytic.t_test`<br>`power_analysis.analytic.factorial_anova`<br>`power_analysis.monte_carlo` | 高级规格 → `power_analytic.R` / `power_monte_carlo.R` | power, sample size/effect size 反解, MCSE, Wilson CI, 失败模拟数 | `test_power_*.py` |
| 通用多重插补 | `ImputationWizard` | `multiple_imputation.mice_dataset_generation`<br>`multiple_imputation.rubin_pooling` | 高级规格 → `imputation_runner.R` / `mi_rubin.R` | 不可变插补数据、Qbar/Ubar/B/T/FMI/Barnard–Rubin df | `test_imputation_*.py` |
| 高级问卷测量 | `QuestionnaireMeasurementBuilder` | `questionnaire_measurement.reliability`<br>`questionnaire_measurement.efa`<br>`questionnaire_measurement.cfa`<br>`questionnaire_measurement.measurement_invariance`<br>`questionnaire_measurement.esem_bifactor_irt`<br>`questionnaire_measurement.common_method_bias` | 高级规格 → `questionnaire_measurement_runner.R` / `esem_bifactor.R` | `methodExecution`, Target, ωh/ECV/PUC, 2PL/GRM 题项参数, DIF 与校正 p 值 | `test_esem_bifactor_irt.py`, R `test-statistical-conformance.R` |

### 机器活动 slice 完整清单

以下清单与 `apps/api/app/capability_catalog.py::ACTIVE_CAPABILITIES` 一一对应。除明确标注的 MICE 生成消费者外，其余均为产品可见；“出现于清单”只证明活动代码登记，不替代上下文适用性、显式规格和实际结果证据。

```text
experimental_design.factorial_anova.long.single_outcome
experimental_design.ancova.long.single_outcome
experimental_design.repeated_measures.single_within
experimental_design.mixed_design.single_within
experimental_design.glm_cluster.long.single_outcome
multilevel_model.aggregation.icc_rwg
multilevel_model.gaussian.two_level
longitudinal_model.observed_growth
longitudinal_model.traditional_clpm
longitudinal_model.ri_clpm
longitudinal_model.latent_growth
longitudinal_model.longitudinal_invariance
power_analysis.analytic.regression
power_analysis.analytic.t_test
power_analysis.analytic.factorial_anova
power_analysis.monte_carlo
multiple_imputation.mice_dataset_generation  [productVisible=false; API consumer only]
multiple_imputation.rubin_pooling
questionnaire_measurement.reliability
questionnaire_measurement.efa
questionnaire_measurement.cfa
questionnaire_measurement.measurement_invariance
questionnaire_measurement.esem_bifactor_irt
questionnaire_measurement.common_method_bias
empirical.cross_sectional.overview
empirical.cross_sectional.measurement
empirical.cross_sectional.group_comparison
empirical.cross_sectional.hierarchical_regression
empirical.cross_sectional.response_surface
model.process_catalog
model.sem
empirical.panel.clpm
empirical.panel.ri_clpm
empirical.panel.lcm_sr
empirical.panel.invariance
empirical.panel.ulmc_sensitivity
empirical.diary.quality
empirical.diary.lmm
empirical.diary.glmm
empirical.diary.cross_classified_gaussian
empirical.diary.multilevel_mediation
empirical.diary.dsem
empirical.diary.mi
empirical.diary.power
```

## 共同前置：数据、变量与测量

| 环节 | 当前能力 | 前置 / 使用方式 | 生成物与边界 |
| --- | --- | --- | --- |
| 数据导入 | CSV、XLSX、SAV、DTA、POR | 在“数据与测量”拖放或选择文件；XLSX 可重选工作表 | 原始文件 SHA-256、本地只读数据版本；不原地覆盖原文件 |
| 数据画像与质检 | 行列数、缺失、唯一值、变量建议和数据质量工作区 | 导入后先检查，再进入变量确认 | 诊断用于决策，不自动修改原数据 |
| 变量字典 | 确认连续、Likert、二元、名义、顺序等类型 | 必须确认后才开放构念分组和计分 | 未确认变量不能绕过测量门禁 |
| 多源 / 多波次合并 | 合并多波次或其他数据源向导 | 在数据摘要处进入；应先明确匹配键和波次语义 | 生成新数据版本，不篡改原版本 |
| 测量版本 | 构念题项分组、反向题、计分、项目与基础信度诊断 | 数据字典确认后配置 | 派生 score variables；后续实证和模型画布使用明确的测量版本 |

## 横截面问卷实证

入口为“横截面问卷 → 问卷与实证分析”。配置区有引导模式和专家模式：前者按研究问题推荐方法并显示前置条件，后者可展开全部当前设置。点击“运行问卷实证分析”后，结果页只将本次报告实际包含的 segment 标为“有结果”。

| 功能 | 配置入口 | 可执行内容 | 结果 | 边界 |
| --- | --- | --- | --- | --- |
| 描述、缺失与分布 | 基础分析与样本 | 样本规模、缺失、描述统计、偏度/峰度、因子分析适切性 | `描述与正态性` | 分布诊断不是自动删样本或变换命令 |
| 相关分析 | 基础分析与样本 | Pearson、Spearman、基于控制变量的偏相关；显式选择 none/Holm/BH，默认 BH | `相关与矩阵`、热图和虚拟化相关表；raw/adjusted p 与 family 证据 | 相关不自动表述为因果；nested 只显示描述系数，不生成逐行 p/普通区间 |
| EFA | 测量模型 | Varimax/Promax；Kaiser、平行分析或手动因子数 | 载荷、共同度、结构矩阵、因子相关、碎石图 | 完整案例或题项变异不足时明确不可用；回退必须标明实际方法 |
| CFA 与基础信效度 | 测量模型 | CFA、α、ω、CR、AVE、Fornell–Larcker、HTMT Bootstrap CI | `信效度 (EFA/CFA)` | 连续 MLR、有序 WLSMV；混合尺度无显式异质相关规格时拒绝；HTMT 披露 Pearson/polychoric 来源；高级 ESEM/Bifactor/IRT/DIF 不在基础报告自动执行 |
| 组间差异 | 基础分析与样本的组间差异变量 | 两组/多组比较、效应量、Welch/Brown–Forsythe、Tukey/Bonferroni/Games–Howell；跨构念 family 默认 Holm | `组间与聚合` | 未选择 `groupVariableId` 即为“未配置”；nested 阻断普通组间检验；raw/adjusted p 同时保存 |
| 聚合诊断 | 独立的 cluster 聚合变量 | ICC(1)、ICC(2)、设计效应、rwg(j) | `组间与聚合` | `aggregationVariableId` 不替代组间比较变量；只提供聚合证据，不自动把数据升级为多层因果结论 |
| 多组测量等值性 | 分组变量 + 测量模型 | 当前报告中的多组测量等值性结果 | `信效度 (EFA/CFA)` | 分组变量和测量结构必须明确；结果与独立高级测量工作台的等值性规格不同，不能混用 |
| 分层 OLS 回归 | 回归与高级分析 | 区块 1 控制变量、区块 2 核心预测构念、ΔR²/F-change、系数、VIF、f²、置信区间 | `分层回归` | 必须先选结果变量和至少一个预测变量；未配置时明确显示“本次未生成分层回归” |
| 回归稳健性与相对重要性 | 回归与高级分析 | 已返回的稳健性诊断；多预测变量时的相对重要性 | `分层回归` / `高级与稳健性` | 仅消费本次报告字段，不合成图或数值 |
| 响应面分析 | 回归与高级分析 | 两个预测变量的响应面规格与结果 | `高级与稳健性` | 必须显式选两个预测变量；不是任意多变量非线性建模平台 |

## 条件过程模型与 SEM

入口为“横截面问卷 → 条件过程模型”。这是独立模型画布，不等同于横截面报告的分层回归页。

| 功能 | 当前能力 | 状态与边界 |
| --- | --- | --- |
| PROCESS 5.0 拓扑识别 | 识别 1–22、28–29、58–73、75–76、80–92，共 55 个当前预编程编号；合法自定义拓扑单独分类 | active；识别不是因果识别 |
| PROCESS 编号兼容估计 | 55 个有效目录编号均可搜索、预览并直接应用，允许配置各自范围内的中介数量；保留 14 个快捷模板及自由画布反向匹配 | active；独立实现并经过全编号执行、方程级 NumPy 与代表性官方宏金标准验证；不宣称复制官方宏全部选项 |
| OLS / logistic 路径 | 画布规格驱动的线性或二元结局路径 | active；二元结局不将 logistic 系数解释为线性概率效应 |
| 中介、调节与条件间接效应 | Bootstrap 间接效应、简单斜率、Johnson–Neyman 区间和交互图（在模型规格适用时） | active；解释必须服从横截面/研究设计边界 |
| SEM | 同一模型工作台中的 `SemStudio` | active；输出模型语法、估计器、拟合、标准化/非标准化路径、收敛和异常解；不使用未接线的独立 Modal |

## 纵向面板

入口为“纵向面板 → 纵向面板分析”。用户应先声明被试 ID、波次、时间值、观测得分或题项级潜变量结构，再选择模型；结果位于独立“纵向面板”页签，数据源是持久化报告 segment。

配置工作流按研究范式隔离：横截面页展示相关、多重性、组间、聚合和回归控件；纵向/日记页只展示样本与非 IID 边界，再进入 `LongitudinalPanelConfig` 或 `DiaryMultilevelConfig`。范式切换会清空不可见的横截面选项，服务端再次按实际 slice fail-closed，R 引擎最后阻断逐行横截面推断。三层边界共同保证“未显示、未请求的方法不执行”。

| 功能 | 前置与配置 | 当前输出 | 边界 |
| --- | --- | --- | --- |
| 波次映射 | 被试 ID；每波 X/Y 变量或等价题项；可设置不等距 `timeValue` | 波次样本流和规格记录 | 波次语义不能由列名猜测替代 |
| 传统 CLPM | 至少 2 波；观测得分或题项级结构 | 自回归、交叉滞后、缺失策略和诊断 | 当前前端说明两时点可启用；复杂设计解释仍需研究设计支持 |
| RI-CLPM | 至少 3 波，推荐的默认模型 | 随机截距、个体内自回归/交叉滞后、竞争与稳健性模型 | 仅限当前声明的双构念结构和估计边界 |
| LCM-SR | 至少 5 波；题项级潜变量；线性或二次宏观生长 | 宏观增长与结构化残差动态结果 | 不等于任意潜变量动态 SEM |
| 纵向测量等值性 | 至少 2 波、题项级潜变量；configural/metric/scalar/strict，可选部分等值 | 级联检验与潜均值比较（适用时） | 题项尺度、估计器和缺失处理须在规格中明确 |
| 纵向 ULMC、竞争模型与功效 | 在相应设置启用 | ULMC 敏感性、竞争/稳健性模型、Monte Carlo 功效/偏差/覆盖率 | 仅报告实际配置和成功运行的模块 |

估计器与缺失处理会随题项尺度变化：连续题项可选 ML/MLR 与 FIML/完整案例；有序题项走 WLSMV 和相应完整案例口径。模型能计算不代表波次可比性、样本流或缺失机制已经得到理论保证。

## 日记 / ESM 与多层动态

入口为“日记 / ESM → 日记 / ESM 分析”。结果位于独立“日记 / ESM”页签，仅展示当前报告中的真实结果、图形和诊断。

| 功能 | 前置与配置 | 当前输出 | 边界 |
| --- | --- | --- | --- |
| 时间质检与合规性 | 被试 ID、时间变量、预期观测数/间隔、答题时长和窗口规则（可选） | 合规性、多层信度、样本/时间诊断 | 不能用固定示例轨迹代替真实 ESM 数据 |
| within / between 分解 | 人均中心化、总均值中心化或不中心化；可声明时间原点和线性/二次时间 | 个体内偏差、个体均值成分和执行记录 | 必须解释中心化和时间/滞后排序规则 |
| 两层 Gaussian LMM | X、Y、随机斜率、独立或 AR(1) 残差、Level-2 协变量 | 固定/随机效应、AR(1)、诊断、稳健性、功效（适用时） | 两层为主要支持范围 |
| 二元 / 计数 GLMM | outcome family、计数模型、暴露量、零过程、分布模拟诊断 | 二元、计数、零膨胀、Hurdle 结果；过度离散和 excess-zero 证据 | 不宣称任意 GLMM、CR2 或一般化多层中介 |
| 交叉分类 | 非中介、非 DSEM 的模型可声明交叉分类变量 | 当前规格允许的交叉随机效应证据 | 不是任意层级或连续时间结构 |
| 多层中介 | 1-1-1 或 2-1-1；X、M、Y 和中心化规则 | within-person 与 between-person 间接效应 | 只支持登记的两层中介结构 |
| 二层 MI 与功效 | LMM 路径中的缺失和 person-by-occasion 规划设置 | 二层插补和 Monte Carlo 人数×时点功效（适用时） | 不替代通用 MI 的下游模型合并 |
| Bayesian DSEM | T≥20；滞后动态、MCMC 链、先验、后验预测和敏感性设置 | rank-normalized/folded R-hat、bulk/tail ESS、MCSE、PPC、先验敏感性、限量 trace/density 图和 Excel 附录图 | 仅观测变量 DSEM；不是潜变量/高阶动态 SEM |

## 高级工作台：实验性但可运行的受限切片

进入顺序固定为“实验设计 → 通用功效与精度 → 通用多重插补 → 高级问卷测量”。每个方法均经过：**规格配置（向导或高级 JSON）→ 验证规格与警告 → 提交后台任务 → 进度/取消/会话恢复 → 结果与可追溯导出**。以下全部为 `experimental`，不得泛化超出登记边界。

### 实验设计

| 可执行切片 | 当前范围 | 主要结果 / 辅助证据 |
| --- | --- | --- |
| 长格式 factorial ANOVA | 单一 outcome、最多 3 个组间因子；单一组间因子可用真实 Games–Howell | ANOVA、EMM、CI、计划对比和适用事后比较 |
| 长格式 ANCOVA | 单一 outcome、长格式 | ANCOVA；协变量×处理交互仅生成警告 |
| 单一组内因子重复测量 | 一个 within factor | EMM/CI、Greenhouse–Geisser 与完整被试内单元检查 |
| 单一组内因子混合设计 | 一个 within factor | EMM/CI、Greenhouse–Geisser、空/缺失单元边界 |
| 长格式 cluster-robust Gaussian GLM | 单一 outcome、identity-link Gaussian lm、CR0 cluster-robust SE | 聚类稳健双侧推断；临界值使用 `1-(1-confidenceLevel)/2`；不冒充 CR2 或 GLMM |

当前未接入操纵检验、基线平衡结论和 CONSORT 样本流。CONSORT 需要逐组的分配、接受干预、失访、排除及原因数据；本项目不从最终分析样本虚构这些字段。

### 通用功效、精度与 MDES

| 可执行切片 | 当前范围 | 输出 |
| --- | --- | --- |
| 回归解析功效 | Cohen f² / R² change、双侧、样本量或效应量反解 | 功效、所需样本量、ceil 回代 |
| t 检验解析功效 | 单样本或两独立样本、Cohen d、双侧 | 功效、所需总样本量 |
| 均衡组间 ANOVA 解析功效 | Cohen f、均衡组间 | 功效、样本量 / 效应量反解 |
| Monte Carlo 功效 | 显式 Gaussian 回归或均衡组间 ANOVA DGP | 有效/失败模拟数、功效、MCSE、Wilson 区间、敏感性曲线 |
| 精度与 MDES | 当前功效向导中的 CI 宽度与最小可检测效应设置 | 精度目标和敏感性结果 |

### 通用多重插补

| 可执行切片 | 当前范围 | 输出与边界 |
| --- | --- | --- |
| 分析绑定的 MICE FCS | 类型感知插补、显式线性下游模型、固定次数/迭代、链与分布诊断 | 不可变插补数据集；核心 Y、预测变量和协变量进入每个插补模型 |
| Rubin 合并推断 | 冻结的线性回归下游模型；逐份拟合 | Qbar、Ubar、B、T、FMI、Barnard–Rubin df；不支持任意模型自动合并，也不包含 D1/D3 多变量检验 |

### 高级问卷测量与共同方法偏差诊断

| 可执行切片 | 当前范围 | 输出与边界 |
| --- | --- | --- |
| Ordinal reliability 与结构性缺失 | 明确题项/构念 | α/ω、ordinal α/ω、构念级结构性缺失 |
| Polychoric EFA、MAP 与 split validation | 显式因子数和旋转 | MAP、载荷、因子相关、split congruence |
| CFA ML/MLR/WLSMV | 明确简单结构；连续或有序题项 | 拟合、收敛/Heywood 状态；小样本和异常解不被掩盖 |
| 多组等值性与潜均值 | 明确 `groupVariableId` | configural/metric/scalar/strict 级联和潜均值比较 |
| ESEM、Bifactor、IRT/DIF | 连续 ESEM 使用由声明构念成员关系生成的 Target；Bifactor 支持连续 ML/MLR 与有序 WLSMV；IRT 使用 MML，二元题运行 2PL、多分类题运行 GRM | ωh、ECV、PUC、区分度/难度阈值、二元或有序 DIF；缺失题项与 DIF 分组共用同一完整案例掩码；有序 ESEM 尚未开放，不是完整自适应测验平台 |
| Marker Variable 与 ULMC | Marker 变量或未测量潜方法因子嵌套模型 | Lindell–Whitney 调整和 ULMC 模型比较；只作当前登记的 CMB 诊断 |

## 结果、警告与导出规则

| 对象 | 当前规则 |
| --- | --- |
| 实证结果页签 | 描述、相关、测量、组间、回归、高级、纵向、日记共 8 个分区；显示“有结果 / 需关注 / 未配置”，不因组件存在假装已运行 |
| 方法执行记录 | 请求方法、实际方法、估计器替换、回退、收敛失败、Heywood、缺包和样本删除均应可见 |
| 专属图形 | MICE 链、功效曲线、Bifactor、IRT/DIF、纵向与日记图仅消费真实 result bundle 字段；不造示例轨迹或插值结论 |
| 可复现导出 | 绑定数据/测量/规格/结果身份、引擎方法与版本、种子、样本流、警告、回退和诊断；图表、文字和导出引用同一结果 bundle |
| 报告事实与披露 | `reportFacts` 只保存当前结果 JSON Pointer，不复制统计值；APA JARS/STROBE/CONSORT/AEA 画像是明确标注范围的披露子集，不认证质量或因果性 |
| 回放与最终门禁 | 基础模型/高级分析包根含 RO-Crate 1.3 元数据、SHA-256 manifest 和校验器；实证中心暂标 replay unavailable；`publicationGate` 分开显示 capability/run/reporting/人工确认 |

## 不属于当前可用范围

- 任意未在 registry 登记的高级方法组合；
- 任意未命中 PROCESS 5.0 编号的自定义拓扑直接运行；
- 三层或任意交叉分类结构、通用广义多层中介、连续时间模型、潜变量/高阶动态 SEM；
- 通用 MI 对任意下游模型的自动合并；
- 准实验因果识别：fixed effects、DiD/event study、IV/2SLS、RDD、IPW/overlap、synthetic control；
- 研究项目、预注册、协作、云端账户、遥测和远程同步；
- 资产包中的研究项目、认证实验室或旧原型。

## 当前优先级

### P0：本轮已闭环；持续作为发布门禁

以下并非“一次性永久完成”的产品声明，而是已经进入活动代码和 Full 门禁的持续控制项。

- [x] **R 数值漂移基线**：Full 在隔离临时目录独立重算 7 组参考金标准，逐字段校验包版本、数据 SHA、JSON 形状和登记的数值容差；不会改写受版本控制的夹具。R 层另有官方 PROCESS 5.0 宏五模型金标准（`engine/R/tests/reference/process-goldens.json`，87 条 R 断言逐项对照）。
- [x] **请求、执行、警告与导出一致性**：执行失败保留稳定错误码、细节和可操作建议；方法请求、实际执行/回退与解释边界进入结果和导出测试。
- [x] **capability registry 与前端一致性**：高级工作台仅显示 `executionAvailable=true` 且含可执行切片的方法族，并由 Web 测试防止“目录可见但不可运行”。
- [x] **路径、对象绑定与 segment**：不可用 payload 不再误判为有实证结果；结果文件、状态文件和 SQLite 的原子发布窗口按精确任务身份与受控路径读取，避免“状态成功、结果 404”的错误呈现；并行资源测试只核验本引擎拥有的临时根目录。

每次 R 包升级、契约改动或发布仍必须执行 Quick、Full 和相关专项测试；不能因本轮闭环而跳过上述检查。

### P1：本轮已加固的实验性切片

- [x] **夹具与失败路径**：二元 2PL 与多分类 GRM IRT/DIF 分别有活动规格夹具，并覆盖题项缺失后的 DIF 分组对齐；同时保留 8 组解析功效产品金标准，以及 MI 缺列、Monte Carlo 无效 DGP、ESEM/Bifactor/IRT 边界等回归测试。
- [x] **可操作提示**：实验、功效、MI、测量、纵向和多层错误保留稳定代码、原始诊断与中文建议；前端任务进度区直接展示建议，不把小样本、未收敛或缺包伪装为成功结果。
- [x] **资源与失败控制**：Monte Carlo 功效在执行前估算工作量和内存，报告可用模拟中的失败数；MICE 对行数、变量数、插补次数和迭代次数实施资源预算，并在超限或估计失败时明确中止。

这些切片仍是 `experimental`：复杂抽样、极端小样本、未登记的下游 pooled model 或超出资源预算的模拟不会被自动宣称为支持范围。

### P2：明确需求后再恢复

资产包恢复必须同时完成当前契约适配、活动 route/registry、R runner、真实前端消费者、结构化结果与导出、专项测试、Quick/Full 门禁及本文和 `project.manifest.json` 更新。文件存在、历史截图或原型不能单独证明能力完成。
